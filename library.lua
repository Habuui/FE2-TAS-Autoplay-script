-- Biblioteca de TASs do FE2
local TAS_LIBRARY = {}

function TAS_LIBRARY.getTAS(mapName)
    -- Aqui você vai adicionar os dados de cada mapa depois de gravar
    local tasData = nil
    if mapName == "Lost_Woods_Easy" then
        -- tasData = { -- seus frames aqui -- }
    elseif mapName == "Castle_Tides_Easy" then
        -- tasData = { -- seus frames aqui -- }
    end
    return tasData
end

return TAS_LIBRARY
